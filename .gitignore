# Python virtual environment
.venv/

# PyInstaller artifacts
build/
dist/
*.spec
DictatorShipping-windows.zip
DictatorShipping-windows.tar.gz

# Compiled Python
__pycache__/
*.py[cod]
*.pyo

# Launcher artifacts
.shortcut_created

# Generated icon (rebuilt from source JPG automatically)
DictatorShipping.ico

# User data & config (privacy — contains personal settings and history)
# These live in %APPDATA%\DictatorShipping\ at runtime, not in the repo,
# but guard against accidental inclusion
settings.json
history.json
error.log

# macOS
.DS_Store

# Windows
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp

# Local session notes (keep locally, not in repo)
*.txt
!requirements.txt

LOCAL*